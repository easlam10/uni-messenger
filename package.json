{
  "name": "uni-messenger",
  "version": "1.0.0",
  "description": "University message sender application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "mkdir -p ./.cache && if [ -d /app/.cache/puppeteer ]; then mv /app/.cache/puppeteer ./.cache; fi"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "mongoose": "^8.0.3",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.31.0",
    "wwebjs-mongo": "^1.1.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": "18.x"
  }
}
